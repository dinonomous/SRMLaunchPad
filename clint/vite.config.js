import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default {
  server: {
    host: '0.0.0.0', // or a different IP address
    port: 5174, // or a different port number
  },
};

